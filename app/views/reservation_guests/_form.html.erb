<%= form_with(model: reservation_guest) do |form| %>
  <% if reservation_guest.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reservation_guest.errors.count, "error") %> prohibited this reservation_guest from being saved:</h2>

      <ul>
        <% reservation_guest.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :reservation_id, style: "display: block" %>
    <%= form.text_field :reservation_id %>
  </div>

  <div>
    <%= form.label :guest_name, style: "display: block" %>
    <%= form.text_field :guest_name %>
  </div>

  <div>
    <%= form.label :contact_number, style: "display: block" %>
    <%= form.text_field :contact_number %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
